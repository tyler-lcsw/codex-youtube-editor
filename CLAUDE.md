# Historical compatibility pointer

This fork runs with Codex. Read `AGENTS.md` and `.agents/skills/`.
The upstream Claude workflow is preserved in Git history at the recorded baseline in
`docs/upstream/baseline.json`; historical Claude UI examples remain valid video subjects.
