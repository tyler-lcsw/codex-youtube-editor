# Bounded Codex acceptance results

September 5, 2026. App-bundled Codex CLI 0.153.4 on M4. These are read-only repository reasoning checks, separate from local media/render tests. Raw reports, event logs and receipts remain under ignored `work/acceptance/`.

| Scenario | Astra | Sol | Review outcome |
|---|---|---|---|
| Initial workflow discovery | Completed | Completed | Correct local skills, scoped hybrid path, memory and publication boundaries |
| Timeline effects | Completed | Completed | Correct master/output clocks, exclusive end frames, failure preservation and CSS/FFmpeg distinction |
| Initial recovery review | Completed | Completed | Exposed dispatch ambiguity and lack of explicit corrupted-import repair; Astra also identified surviving child processes |
| Recovery after fixes | Completed | Completed | Recognized one-time dispatch, separate import repair and whole-group cleanup; preserved remote-call uncertainty |

The first timeline review found stale status prose; status documentation was rewritten. The insert-clock coverage gap was addressed with a real pixel/frame render test. Initial recovery findings led to dispatch markers, separate import attempts, approval scope checks and a cancellation regression with an orphaned TERM-ignoring child. The follow-up Astra review found the cached-import fast path skipped current scope validation; that path was corrected and a revoked-approval regression now passes.

Sol noted that changed reference files must be restored exactly or handled as a new approved request. This remains the deliberate reference-hash boundary, not permission to relabel an old artifact. Neither model claimed that a dispatch marker proves remote generation completed, that cache utilities wrap every CLI, or that cleanup is full process checkpoint recovery.

The runner retains `completed_unreviewed` in raw receipts. The observations above record this development review; they do not claim human creative approval, live hosted generation, or a long-form production benchmark. Later code changes are covered by focused regressions rather than silently rewriting earlier reports.
